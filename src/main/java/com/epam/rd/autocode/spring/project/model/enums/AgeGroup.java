/** Цей файл визначає основні типи та поведінку для цього модуля */
package com.epam.rd.autocode.spring.project.model.enums;

/**
 * Перелік (enum), який визначає вікову категорію книги
 * Використовується для класифікації книг за віком читача
 */
public enum AgeGroup {

    // Для дітей (дитяча література)
    CHILD,

    // Для підлітків
    TEEN,

    // Для дорослих
    ADULT,

    // Інша категорія (якщо не підходить під основні)
    OTHER
}